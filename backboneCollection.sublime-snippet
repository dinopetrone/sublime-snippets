<snippet>
    <content><![CDATA[
define(function( require, exports, module ){
    var Backbone = require('backbone'),
        ${1:Model} = require('models/${1:Model}');

    return Backbone.Tastypie.Collection.extend({
        ${2://}urlRoot: '${3:/api/v1/auction_item/}',
        model: ${1:Model}
    });
});
]]></content>
    <tabTrigger>collection</tabTrigger>
    <scope>source.js</scope>
</snippet>
